#
# Profile-specific configuration for non Windows systems
#

# Path to where we store csv files 
sc.csvPath=/home/csv

# Path to where we store (serialized) spaces 
sc.exePath=/home/spaces

# Path to temporary folder 
app.tmpPath=/home/tmp
