#
# Profile-specific configuration for Windows systems
#

# Path to where we store csv files 
sc.csvPath=C:/csv

# Path to where we store (serialized) spaces 
sc.exePath=C:/spaces

# Path to temporary folder 
app.tmpPath=C:/tmp
